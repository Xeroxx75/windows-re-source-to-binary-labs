# 05 - DLL and Dynamic Imports

Planned labs:

- call imported Win32 APIs directly;
- load a DLL with `LoadLibrary`;
- resolve a function with `GetProcAddress`;
- compare static imports and dynamic behavior.
